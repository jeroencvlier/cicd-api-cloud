import setuptools

setuptools.setup(
    name="cicd",
    version="0.1.0",
    packages=setuptools.find_packages(),
    author="Jeroen van Lier",
    # use any version of only Python 3.8.x
    python_requires="~=3.8",
)
